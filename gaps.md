# Furlund (karkasni-budynky) — відкриті питання та припущення

Демо-патерн для ніші «каркасні будинки», вигаданий бренд Furlund (Київ). Список нижче — явні
припущення/плейсхолдери, зроблені за відсутності фактів, і пункти, що потребують подальшої дії.

## 1. Контактні дані — плейсхолдери
- Телефон `+380 44 000 00 00`, email `studio@furlund.ua`, Telegram `@furlundstudio`, WhatsApp —
  усі свідомо позначені як шаблон (нулі в номері), не реальні дані реальної людини чи компанії.
- Адреса студії навмисно НЕ вказана точно (лише «Київ, Україна — за попереднім записом»), щоб не
  вигадувати неіснуючу вулицю/номер будинку як «реальний» факт.
- **Дія при переспрямуванні патерна на реального ліда:** замінити на реальні контакти клієнта.

## 2. Домен `furlund.ua`
- Використовується у canonical/OG/schema URL. Не перевірено, чи вільний цей домен у реальності —
  не публікувати зовні (GitHub Pages тощо) без окремого підтвердження власника, як і вказано в задачі.

## 3. Нові фото модулів моделі (models.html) — часткова поставка [ІНТЕГРОВАНО]
- У `source-assets/` з'явилися `model-lyuks.jpg`, `model-perehid.jpg`, `model-vognysche.jpg`
  (промпти — `nano-banana-process-prompts.md`). За прямою вказівкою оркестратора (2026-08-17) — усі
  3 файли інтегровані на `models.html`, замінили `interior.webp` / `winter.webp` / SVG-заглушку на
  картках «Лінія. Люкс», «Лінія. Перехід», «Лінія. Вогнище» відповідно. Оптимізовані в
  `assets/images/model-*.webp` (+ `-md` варіанти), alt-тексти переписані під реальний зміст фото.
- Фото для «Лінія. Студія» (заміна `detail.webp`) все ще відсутнє в поставці — картка НЕ займана,
  чекає окремого завдання, коли фото буде готове.

## 4. Флагманський проєкт «Бурштинова галявина» — концептуальні дані
- Локація, площа (≈240 м²), рік («Концепція, 2024») — явно позначені як концепція, не подані як
  факти реального збудованого об'єкта (компанія вигадана, це прямо прописано в брифі §9 і в
  дизайн-рішенні секції «Параметри, а не обіцянки» на головній).

## 5. Модуль «Лінія. Студія» (models.html) — все ще детейл-фото
- Наразі використовує `assets/images/detail.webp` (кадр деталі вікна/фасаду), не показує сам
  модуль цілком. Фото для заміни ще не згенероване/не надане — окреме завдання, коли буде готове
  (єдиний відкритий пункт з п.3 після інтеграції решти 3 фото 2026-08-17).

## 6. Візуальна QA — обмеження середовища
- **Оновлено 2026-08-17:** у цій сесії Browser pane таки рендерив стилізовану сторінку (styles.css
  підключався, зображення й відео завантажувались, живі скріншоти отримані), але сам інструмент
  скролу (`computer{action:"scroll"}`) був вкрай нестабільним — регулярні таймаути з помилкою
  «Browser pane is currently hidden», випадкові композитні/биті кадри, спорадичні мимовільні
  переходи на інші сторінки при скролі, і повністю зламаний рендер при кастомних (не-preset)
  розмірах viewport (напр. 1440×900 показував сторінку стиснутою в маленький прямокутник у куті
  канви). Обхідні прийоми, що спрацювали: повторні `wait` після таймауту скролу, невеликі кроки
  скролу з проміжними скріншотами, `resize_window` з `preset` замість довільних width/height,
  і — коли `[data-reveal]` fade-in анімація (IntersectionObserver) не встигала спрацювати після
  синтетичного скролу — тимчасовий CSS-оверрайд `[data-reveal]{opacity:1!important}` **лише для
  QA-скріншотів**, видалений одразу після перевірки (не потрапив у фінальний код).
- Цим раундом підтверджено живими скріншотами: full-bleed фото «Ліс не за вікном» (десктоп),
  нова секція «Одна стіна» з 2-колонковим CSS-патерном (мобільний viewport — фото й вертикальний
  стек послідовно, десктопний 900px+ режим підтверджений лише статично через CSS-збіг з уже
  перевіреним на сайті `material-split`), hero H1, автоплей відео на technology.html/about.html,
  нові фото на models.html, мобільна розкладка 375px без горизонтального скролу.
- **Залишається дія для наступного агента/QA (Діма) або власника:** один живий десктопний
  (900px+) прогляд секції «Одна стіна» саме в реальному браузері (не в цьому sandbox Browser pane)
  перед фінальною здачею клієнту — щоб на 100% підтвердити 2-колонковий stretch-layout, який тут
  перевірений лише статично для десктопної ширини.

## 7. Вага сторінки
- Домашня сторінка важча за типовий landing (~1.5 MB з `standards/code.md`) через відео-hero
  (~2.6 MB десктоп-версія drone-відео) — свідомий компроміс, узгоджений із самим брифом (розділ 5:
  «cinematic architectural opening», «великоформатне зображення/відео» як обов'язкова вимога
  hero). Усі відео стиснуті (`libx264`, `crf 26–27`) і не мають аудіодоріжки.

## 8. Відео «Прогулянка територією» (amber-hollow.html) — не підтверджено живим скріншотом
- **Додано 2026-08-17 (раунд #3).** Власник повідомив, що відео нібито «зникло» з сайту. Перевірка
  коду показала, що воно НІКОЛИ не видалялось — `<video autoplay muted loop playsinline
  preload="none" poster="...">` з `<source src="assets/video/walkthrough.mp4">` присутнє в секції
  «Прогулянка територією» весь час, файл валідний (3.7MB, H.264 High/yuv420p, moov-атом на початку
  файлу — faststart, ідентично іншим відео сайту, підтверджено `ffprobe`), сервер віддає 200 OK,
  перший кадр відео (перевірено через `ffmpeg`-екстракцію) — яскраве денне фото дерев'яного
  настилу між об'ємами, не темне.
- Знайдена й виправлена реальна технічна прогалина: на відміну від hero-відео (яке має явний
  `.load()`+`.play()` виклик у `script.js`), відео на amber-hollow.html/technology.html/about.html
  покладались лише на нативний `autoplay` браузера без явного запуску — ненадійно для відео з
  `preload="none"`, що стартує при скролі, а не на завантаженні сторінки (Safari особливо схильний
  просто лишати постер назавжди без явного `.play()`). Додано узагальнений `IntersectionObserver`-
  хук у `script.js` для всіх `video[autoplay]:not([data-hero-video])` — той самий перевірений
  патерн, що й у hero, але лінивий (rootMargin 200px).
- **Не вдалось на 100% підтвердити живим скріншотом**, що відео реально грає після фіксу — Browser
  pane цієї сесії був вкрай нестабільний саме на скролі до цієї секції (регулярні таймаути «Browser
  pane is currently hidden»), і секція консистентно показувала суцільний темний прямокутник навіть
  після 6+ секунд очікування в кількох окремих спробах з різних viewport (375px і 1440px). Це
  узгоджується з тим, що навіть текстові `[data-reveal]`-блоки в цій самій секції не завжди встигали
  проявитись при синтетичному скролі (див. п.6 вище) — ймовірний висновок: те, що виглядало «чорним
  відео», було насправді не-проявленим (`opacity:0`, `filter:blur(14px)`) `.media-frame` з
  `data-reveal="scale"` на темному фоні секції, а не збій самого відео чи файлу.
- **Дія:** рекомендується один живий перегляд власником у звичайному браузері (не в sandbox Browser
  pane) перед показом клієнту — щоб остаточно зняти питання. Код і відеофайл самі по собі коректні
  й не потребують подальших змін, якщо цей перегляд підтвердить відтворення.
- **ОНОВЛЕНО 2026-08-17 (наступна сесія, раунд #4).** Гіпотеза вище («не збій відео, а не-проявлений
  reveal») виявилась НЕВІРНОЮ. Власник прислав живий скріншот зі свого реального браузера: відео
  дійсно на паузі з видимою play-кнопкою — і на `amber-hollow.html` (walkthrough.mp4), і на
  `about.html` («Дім живе цілу добу», day-night.mp4). Корінна причина: виклик `.play()` одразу після
  `.load()`, поки `readyState` ще 0, ненадійний у частині WebKit-збірок — тихо ігнорується без
  помилки в консолі (саме тому в попередній сесії computed styles/JS не показували проблеми).
  Виправлено: `script.js` тепер чекає на подію `canplay`/`loadeddata` перед `.play()` (а не викликає
  одразу), з одноразовим ретраєм якщо перша спроба все ж відхилена; `preload="none"` → `"metadata"`
  для всіх не-hero autoplay відео (`about.html`, `amber-hollow.html`, `technology.html` hero).
  **Підтверджено іншим методом цього разу** — не лише computed styles, а порівнянням двох
  скріншотів з паузою 3–5с між ними: композиція кадру видимо змінилась (камера панорамувала, інший
  об'єм у кадрі) на обох відео, що доводить реальне відтворення. Кастомної play-іконки в CSS/HTML не
  знайдено (перевірено явно) — видима «play-кнопка», яку бачив власник, була нативним
  WebKit-афордансом на паузованому відео, що зникає сам, щойно відео реально стартує.

## 9. Site-wide фікс: `overflow-x` перенесено з `body` на `html`
`body { overflow-x: hidden }` у Chromium/WebKit змушує `overflow-y` обчислюватись як `auto` (CSS
2.1 §11.1.1), через що `body` стає власним scroll-контейнером — і `position: sticky` усередині
перестає працювати ВСЮДИ на сайті (виявлено при діагностиці sticky-фото в секції «Одна стіна», див.
`state.md` розділ «Раунд правок #4», п.1). Виправлено перенесенням правила на `html` — той самий
захист від горизонтального скролу, без побічного ефекту на sticky. Перевірено мобільним QA-проходом
(320–390px, кілька сторінок) після зміни — горизонтального скролу не виявлено ніде.

## 10. Публікація на GitHub — ЗАВЕРШЕНА 2026-08-17
Репозиторій: **https://github.com/yakovyshynn-afk/furlund-demo** (публічний).
Публічний сайт (GitHub Pages, гілка `main`, корінь репо): **https://yakovyshynn-afk.github.io/furlund-demo/**
Git identity налаштована локально в цьому репо (не глобально): `user.name = "Roman Yakovyshyn"`,
`user.email = "yakovyshynn-afk@users.noreply.github.com"`. Публікація явно підтверджена власником
у чаті й оркестратором перед виконанням (чекпоінт CLAUDE.md «Демо готове — до публікації» пройдено).
Живий Browser pane прогін на самому GitHub Pages URL підтвердив коректний рендер (не sandbox
preview — реальний публічний хостинг).

**Домен `furlund.ua` — нагадування:** усе ще вигаданий плейсхолдер у canonical/OG/schema на всіх
сторінках, не існує в реальності, не куплений і не перевірений як вільний. GitHub Pages URL вище —
**єдине реальне публічне місце**, де цей концепт зараз доступний. Якщо демо піде в реальний аутріч
під конкретного ліда — canonical/OG варто буде або замінити на реальний майбутній домен ліда, або
явно вказати на GitHub Pages URL, залежно від того, що вирішить оркестратор/Соля на етапі показу.
